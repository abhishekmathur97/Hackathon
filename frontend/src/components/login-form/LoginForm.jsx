import styles from './LoginForm.module.css';

const LoginForm = () => {
    return (
        <form className={styles.container}>
            Login/Register
        </form>
    )
}

export default LoginForm;